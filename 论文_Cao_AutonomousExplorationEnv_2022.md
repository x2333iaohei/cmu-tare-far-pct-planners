# Autonomous Exploration Development Environment and the Planning Algorithms (ICRA 2022)
本文是卡内基梅隆大学（CMU）机器人研究所的Chao Cao、Hongbiao Zhu、Howie Choset、Ji Zhang发表于ICRA 2022的工作，系统性地介绍了CMU探索团队（CMU Exploration Team）的**全栈开源自主探索开发环境**——一个集成三层导航架构（全局TARE探索规划器 + 局部FALCO规划器 + 地形可通行性分析）的完整软件系统。该系统在DARPA地下挑战赛（Subterranean Challenge）中荣获**"Most Sectors Explored Award"（最多扇区探索奖，26/28扇区）**，并在Urban Circuit中以自主探索**886米、1458秒**零人工干预的成绩验证了工程实用性。本文不仅是算法论文，更是一份**开放环境的"最佳实践指南"（Best Practices Guide）**，为机器人社区提供了一套从仿真到真实部署的完整探索方案。

---

## 一、核心问题与核心洞察

### 1. 待解决的核心问题

自主探索未知三维环境是一个"研究活跃但工程匮乏"的领域：学术界发表了大量探索规划算法（NBVP、GBP、MBP等），但很少有工作将算法集成到**完整、可部署、鲁棒**的软件系统中。具体的矛盾和挑战包括：

**矛盾一：算法多样性与系统集成性的鸿沟。** 单个探索算法（如NBVP、TARE）在论文的受控实验中可能表现良好，但真实世界的探索不仅需要"下一个视点规划"，还需要：实时定位（SLAM）、局部避障（local planning）、地形评估（traversability）、运动控制、传感器融合、通信中继等。将这些模块集成为同频共振的完整系统，难度远超单个算法的研发。缺少开源参考实现使得每个团队都在重复"造轮子"。

**矛盾二：仿真的"过度简化"与现实的"过度复杂"。** 学术仿真环境（如Gazebo默认Worlds）通常具有理想的地面（完美平坦、均匀摩擦）、均匀光照、无传感器噪声、静态场景。而真实地下环境（矿井、隧道、洞穴）具有：不规则地形（碎石、斜坡、水坑）、极端光照（从完全黑暗到手电筒直射）、粉尘/雾气导致的传感器退化、通信中断（地下无GPS/WiFi）。从学术仿真到真实地下的"环境鸿沟"甚至大于导航领域的sim-to-real gap。

**矛盾三：探索效率与安全性的权衡。** 真实机器人部署有严格的**安全底线**：一次碰撞或跌落（如掉入矿井竖井）可能导致机器人损毁和任务失败。而激进的探索策略（追求高速全覆盖）可能在未知地形上冒险——传感器在60°俯角的有限视场（FoV）中看不到脚下的**负障碍物（negative obstacles，如坑洞、沟渠）**。保守策略（缓慢、频繁停车检查）则导致探索效率低下，无法在有限电池续航和比赛时限内完成探索任务。

**矛盾四：硬件计算约束。** DARPA SubT使用的机器人平台计算资源有限（Intel NUC i7，4-8核，16-32GB RAM），需要同时运行SLAM（如LIO-SAM，3-5ms/scan）、探索规划（TARE, ~210ms/cycle）、局部规划（FALCO, < 10ms）、地形分析（voxel traversal, ~50ms）、状态机管理。所有模块的总计算负载必须严格约束在可用的CPU时钟周期内，否则任何模块的计算超时都会导致控制回路中断。

### 2. 核心设计洞察

**洞察一：三层导航架构的解耦是系统鲁棒性的关键。**

CMU团队将导航栈分解为三个严格按照频率和时间尺度解耦的层次：
- **全局探索层（TARE）：** 低频（0.2-0.5Hz），大尺度（视野50-100m），决定"探索哪个方向"
- **局部规划层（FALCO）：** 中频（5-20Hz），中尺度（视野10-20m），决定"如何安全到达那个方向"
- **地形评估层（Terrain Traversability）：** 高频（10-30Hz），小尺度（视野3-5m），决定"脚下的地面是否可通行"

这种"频率分层"的设计使得：地形评估可以在高频下保证即时安全（防止掉入坑洞），局部规划可以在延迟不敏感的情况下完成平滑轨迹优化，全局探索可以在低频下完成计算密集的视点信息增益评估和TSP规划。各层之间的通信通过共享的环境表示（三维占据栅格 + 二维代价地图）进行。

**洞察二：负障碍物处理是地下探索的"杀手级功能"。**

在平坦的学术仿真中，负障碍物（坑洞、竖井、沟渠）几乎不存在。但在真实地下环境中，它们是机器人损毁的首要原因。CMU团队的地形分析模块通过**体素层面的垂直不连续性检测**，专门识别前方地形中"看似平坦但存在突然下降"的区域，并在代价地图中将负障碍物区域标记为致命代价（lethal cost），强制局部规划器绕行。

负障碍物的物理特征：传感器（LiDAR/深度相机）在水平扫描时，负障碍物的"远壁"可能被探测到（返回测距值），但"近壁"（坑洞边缘）与地面的过渡区域由于传感器俯角限制（典型45-60°）可能不可见。因此负障碍物在原始点云中表现为一段**测距的突然跳变**（distance discontinuity）。

**洞察三：蒙特卡洛运动基元库（Monte Carlo Motion Primitive Library）在非结构化地形中优于梯度优化。**

传统局部规划器（如TEB、MPC）使用基于梯度的数值优化求解器（g2o、IPOPT等），依赖于障碍物代价函数的平滑性和凸性。在非结构化地形（随机碎石、不规则斜坡、狭窄隧道壁）中，代价函数高度非凸且充满局部最小值，梯度优化极易陷入不可行解。FALCO采用"大样本随机采样 + 选择最优"策略：生成 $M = 500$–$2000$ 个运动基元（每个基元是运动学可达的短时轨迹段），在代价地图上对每个基元评分，选择代价最低者。该策略天然对非凸代价函数免疫，且计算量可预测（$\mathcal{O}(M \cdot L)$，$L$为基元长度），避免了梯度优化的收敛不确定性。

---

## 二、核心技术原理

### 1. 系统总览 —— 三层导航架构

CMU自主探索系统的完整架构如下：

```
                    ┌──────────────────────────────────────┐
                    │           DARPA SubT 比赛层            │
                    │    (任务状态机, 通信管理, 多机器人协调)  │
                    └──────────────────┬───────────────────┘
                                       ↓
            ┌──────────────────────────────────────────────────┐
            │                 三层导航架构                      │
            │                                                  │
            │  ┌─────────────┐  ┌─────────────┐  ┌──────────┐ │
            │  │  Global     │  │   Local     │  │ Terrain  │ │
            │  │  Explorer   │  │  Planner    │  │ Analysis │ │
            │  │  (TARE)     │→│  (FALCO)    │←│ (Voxel)  │ │
            │  │  0.2-0.5Hz  │  │  5-20Hz     │  │ 10-30Hz  │ │
            │  └──────┬──────┘  └──────┬──────┘  └────┬─────┘ │
            │         │                │               │       │
            │         ↓                ↓               ↓       │
            │  ┌──────────────────────────────────────────┐   │
            │  │           共享环境表示层                   │   │
            │  │  3D 占据栅格 (OctoMap/Voxblox)            │   │
            │  │  2D 代价地图 (costmap_2d with layers)     │   │
            │  │  探索进度图 (exploration progress map)    │   │
            │  └──────────────────────────────────────────┘   │
            │                     │                            │
            └─────────────────────┼────────────────────────────┘
                                  ↓
                    ┌──────────────────────────┐
                    │       SLAM 定位层         │
                    │  LIO-SAM (LiDAR-Inertial) │
                    │  或视觉-惯性融合           │
                    └──────────────────────────┘
                                  ↓
                    ┌──────────────────────────┐
                    │      传感器驱动层         │
                    │  LiDAR (Ouster/Velodyne), │
                    │  IMU, 深度相机, 轮式里程计 │
                    └──────────────────────────┘
```

### 2. 全局探索规划层 —— TARE (Task-oriented Autonomous Robot Exploration)

TARE作为全局探索规划器的详细原理已在ICRA/RA-L 2021论文（Cao et al.）中完整阐述，本文重点描述其在**完整系统中的集成方式与工程实践**：

**（a）与系统其余模块的接口：**
- **输入：** 从共享3D占据栅格（Voxblox/OctoMap）中获取当前的占据/自由/未知体素分布
- **输出：** 一个全局目标视点 $v_\text{target} = (\mathbf{p}_\text{target}, \xi_\text{target})$，发送给FALCO局部规划器作为"最终目标"（goal tolerance: 位置0.5m, 偏航10°）
- **触发条件（事件驱动）：** 局部探索"耗尽"（局部最大信息增益 < $\tau_\text{global}$ = 5 m²）、机器人到达当前全局目标、或新发现一个信息增益高50%以上的前沿区域
- **更新频率：** 触发时运行，典型间隔10-30秒，单次计算约210ms

**（b）在DARPA SubT中的适配修改：**

| 原TARE设计                  | DARPA SubT适配            | 原因                       |
| ------------------------ | ----------------------- | ------------------------ |
| 局部滚动栅格 20×20×5m, 0.3m分辨率 | 局部栅格 30×30×10m, 0.4m分辨率 | 地下环境更开阔，更粗分辨率补偿了更大的空间范围  |
| 全局栅格 $H=8$               | $H=6$                   | 地下拓扑复杂（多岔道矿井），保持更精细的全局粒度 |
| 传感器模型：深度相机 90° FoV       | LiDAR 360° + 深度相机融合     | 地下黑暗环境RGB-D失效，LiDAR为主传感器 |
| 视点采样：仅位置+偏航              | 位置+偏航+俯仰（3自由度）          | 地下有垂直结构（竖井、斜坡），需要三维视点    |
| 信息增益评估：光线投射（CPU）         | GPU加速光线投射（CUDA）         | 3DoF视点使碰撞检测复杂度增加，需GPU加速  |

**（c）TARE的两个关键模式：**

**探索模式（Exploration Mode）：** 标准TARE行为——视点采样、信息增益评估、局部/全局分层规划。

**回程模式（Relocation Mode）：** 当机器人需要返回基地或进入下一个探索区域时（如比赛分阶段进行），TARE切换到回程模式：
- 停止新视点采样（不寻找新探索目标）
- 在拓扑图中找到从当前位置到目标区域的最佳路径
- 该路径被发送给FALCO执行，沿途最大化"已知区域的穿越效率"
- 回程模式中全局规划器沿用已缓存的拓扑图边代价，无需重新规划

### 3. 局部规划层 —— FALCO (Fast Adaptive Local planner with Monte Carlo motion primitives)

FALCO是CMU团队自研的局部运动规划器，其核心特征是**基于蒙特卡洛采样的运动基元库**，专为应对非结构化、非凸环境中的局部避障而设计。

**（a）问题形式化：**

给定：
- 机器人的当前状态 $\mathbf{x}_0 = (x, y, \theta, v, \omega)$
- 局部代价地图 $\mathcal{C}: \mathbb{R}^2 \to [0, 255]$（0=自由, 254=致命障碍, 255=未知）
- 全局目标 $\mathbf{x}_\text{goal}$（来自TARE）
- 运动基元库 $\mathcal{P} = \{\pi_1, \pi_2, \dots, \pi_M\}$，每个基元 $\pi_k = \{(\mathbf{x}_k^1, \mathbf{u}_k^1), \dots, (\mathbf{x}_k^N, \mathbf{u}_k^N)\}$ 为 $N$ 步运动学前向仿真

FALCO的目标是在 $\mathcal{P}$ 中选择最优基元：
$$\pi^* = \arg\min_{\pi_k \in \mathcal{P}} \; J(\pi_k) = \arg\min_{\pi_k} \left[ w_\text{goal} \cdot d(\mathbf{x}_k^N, \mathbf{x}_\text{goal}) + w_\text{cost} \cdot \sum_{i=1}^{N} \mathcal{C}(\mathbf{x}_k^i) + w_\text{obs} \cdot \mathbf{1}[\pi_k \text{ collides}] \cdot \mathcal{M} \right]$$
其中 $\mathcal{M} \gg 1$ 为致命大数（确保碰撞基元永不被选中）。

**（b）运动基元库的蒙特卡洛生成：**

FALCO的运动基元库以**离线预计算 + 在线采样**的方式构建：

1. **离线预计算（一次性）：**
   在机器人速度空间 $\mathcal{V} = \{(v, \omega) \; | \; v \in [0, v_\text{max}], \omega \in [-\omega_\text{max}, \omega_\text{max}]\}$ 中，通过重要性采样（importance sampling）生成 $M$ 个 $(v, \omega)$ 样本。采样密度在低速、高曲率区域更高（因为这些短急转弯在避障中最常用）。

2. **在线前向传播（每周期）：**
   对采样的每个 $(v, \omega)$，运行 $N$ 步运动学仿真（差速驱动模型）：
   $$\mathbf{x}_{i+1} = \mathbf{x}_i + \begin{bmatrix} \cos\theta_i & 0 \\ \sin\theta_i & 0 \\ 0 & 1 \end{bmatrix} \begin{bmatrix} v_i \\ \omega_i \end{bmatrix} \Delta t, \quad i = 0, \dots, N-1$$
   其中 $\Delta t = 0.05\text{s}$, $N \cdot \Delta t = T_\text{horizon}$（典型值2.0s）。

3. **自适应动态窗口约束：**
   并非所有 $(v, \omega)$ 在所有时间都可执行。FALCO实时计算**动态窗口**（Dynamic Window）——考虑当前速度和加速度极限的可达速度范围：
   $$V_d = \{(v, \omega) \; | \; v \in [v_c - a_\text{max} \cdot \Delta t, v_c + a_\text{max} \cdot \Delta t], \; |\omega| \in [|\omega_c| - \alpha_\text{max} \cdot \Delta t, |\omega_c| + \alpha_\text{max} \cdot \Delta t]\}$$
   其中 $v_c, \omega_c$ 为当前速度，$a_\text{max}, \alpha_\text{max}$ 为最大线/角加速度。只有动态窗口内的基元被评估，将有效候选数从 $M$ 动态缩减至 $M_\text{eff} \approx 200$–$500$。

**（c）代价地图的多层融合：**

FALCO使用的2D代价地图是多个代价层的**逐像素最大值融合**（maximum-of-layers policy）：

$$\mathcal{C}(x, y) = \max\left( \mathcal{C}_\text{static}(x, y), \; \mathcal{C}_\text{terrain}(x, y), \; \mathcal{C}_\text{inflation}(x, y), \; \mathcal{C}_\text{negative\_obs}(x, y) \right)$$

| 代价层                                | 数据来源                | 代价范围           | 更新频率   |
| ---------------------------------- | ------------------- | -------------- | ------ |
| $\mathcal{C}_\text{static}$        | 激光雷达占据栅格投影          | 0（自由）或 254（占据） | 10Hz   |
| $\mathcal{C}_\text{terrain}$       | 地形可通行性分析（步高、坡度、粗糙度） | 0-254（连续代价）    | 5-10Hz |
| $\mathcal{C}_\text{inflation}$     | 障碍物膨胀（机器人半径 + 安全裕度） | 0-253（距离衰减）    | 2Hz    |
| $\mathcal{C}_\text{negative\_obs}$ | 负障碍物检测（坑洞、竖井边缘）     | 253（致命代价）      | 10Hz   |

**融合策略的选择理由：** 最大值融合（而非加权求和）保证了**安全优先**——当任意层将一个区域的代价标记为致命（254），无论其他层给出何种低代价，该区域都会被FALCO规避。这避免了权重调优中可能出现的"加权后致命区域被低代价层稀释"的安全隐患。

**（d）FALCO的计算效率：**

| 计算步骤                                 | 耗时 (典型值)   | 备注           |
| ------------------------------------ | ---------- | ------------ |
| 动态窗口约束                               | < 0.5 ms   | 简单区间运算       |
| 前向运动学传播 ($M_\text{eff}$=300, $N$=40) | 2.8 ms     | 向量化批处理       |
| 代价地图查表 (300轨迹 × 40点)                 | 1.2 ms     | 整数查表，无浮点运算   |
| 基元评分与排序                              | 0.3 ms     | 堆排序取前K       |
| **总计**                               | **< 5 ms** | 满足20Hz控制频率需求 |

**（e）FALCO vs. TEB/MPC的定性对比：**

| 特性         | FALCO (采样-评估)                                 | TEB/MPC (梯度优化)                |
| ---------- | --------------------------------------------- | ----------------------------- |
| 对非凸代价函数的处理 | 天然免疫（采样穷举有效子集）                                | 可能陷入局部最小值                     |
| 计算时间的可预测性  | 确定性（$\mathcal{O}(M_\text{eff} \cdot N)$）      | 依赖于收敛迭代次数，可能超时                |
| 解的最优性      | 近似最优（$M \to \infty$ 时趋向最优）                    | 局部最优（依赖初始解）                   |
| 对障碍物膨胀敏感度  | 低（离散查表）                                       | 高（影响梯度的平滑性）                   |
| 调参难度       | 低（$M$, $N$, $w_\text{goal}$, $w_\text{cost}$） | 高（优化器容差、迭代上限、正则化系数、障碍物代价函数形式） |

### 4. 地形可通行性分析层 —— Terrain Traversability Analysis

CMU团队的地形分析模块是整个导航栈中最贴近硬件的"安全卫士"，其核心功能是在机器人行进方向前方**实时评估地面是否可通行**，特别关注三个危险特征：**负障碍物（坑洞/竖井）**、**不可攀爬的陡坡**、**过高台阶**。

**（a）体素栅格的地形表示：**

环境以体素栅格（voxel grid）表示，体素分辨率 $r_\text{voxel} = 0.1\text{m}$（典型值）。每个体素 $v_{ijk}$ 存储三个状态之一：
- **UNKNOWN：** 尚未被传感器观测
- **OCCUPIED：** 被点云占据
- **FREE：** 经传感器射线验证为自由空间

地形分析在机器人前方的局部窗口中运行（窗口大小：6m × 6m × 2m，机器人坐标系）。

**（b）步高/台阶检测（Step Height Detection）：**

在机器人行进方向的前沿，对每个体素列（沿重力方向的z轴），计算从地面到第一个占据体素的高度差 $\Delta z$：
$$\Delta z(x, y) = z_\text{first\_occupied}(x, y) - z_\text{ground}(x, y)$$

其中 $z_\text{ground}$ 为当前估计的地面高度（由IMU + 机器人已知高度 + 最近地面点云联合估计）。

若 $\Delta z(x, y) > h_\text{max\_step}$（轮式机器人典型值为0.15m，履带式为0.25m），则该 $(x, y)$ 位置被标记为**不可通行**（代价254）。

**（c）坡度分析（Slope Analysis）：**

对地面体素的邻域进行平面拟合（RANSAC），估计局部坡度 $\alpha$：
$$z(x, y) = a \cdot x + b \cdot y + c + \epsilon$$

局部坡度角 $\alpha = \arctan(\sqrt{a^2 + b^2})$。

若 $\alpha > \alpha_\text{max}$（典型值为25°–30°），该区域标记为不可通行。对于差速驱动机器人，还需考虑**侧倾角**（roll angle）——即使纵向可通过，横向坡度过大也可能导致机器人侧翻。

**（d）负障碍物检测 —— 核心安全功能：**

负障碍物（negative obstacle）指地面上的突然下陷：坑洞、沟渠、矿井竖井。其检测算法基于**激光雷达测距的不连续性 + 体素的垂直空隙分析**：

**Algorithm: Negative Obstacle Detection**
```
Input: LiDAR点云 P (机器人坐标系), 体素栅格 V (基于累积点云)
Output: 负障碍物代价层 C_negative_obs

1: 将 P 按方位角等分为 N_azimuth = 360 个扇区（1°分辨率）
2: for 每个扇区 s_i (i = 1, ..., 360) do
3:   提取 s_i 中的点 p_{i,1}, ..., p_{i,K_i}（按距离排序）
4:   for 相邻点对 (p_{i,j}, p_{i,j+1}) do
5:     Δd ← ‖p_{i,j+1}‖ - ‖p_{i,j}‖  // 测距差
6:     if Δd > Δd_threshold (典型值 0.5m) then
7:       // 检测到距离不连续性：p_{i,j} 是近壁，p_{i,j+1} 是远壁（坑洞的彼岸）
8:       后向追踪：从 p_{i,j} 沿扇区方向向内追踪，检查体素列中是否存在垂直空隙
9:       for step = 1 to N_backtrack do
10:        查询体素 v at (p_{i,j} - step · Δr · direction(s_i))
11:        if v.state == UNKNOWN and v.z_depth > z_below_threshold then
12:          标记从 p_{i,j} 到 p_{i,j+1} 的地面区域为负障碍物 (代价=253)
13:          break
14:        end if
15:      end for
16:    end if
17:   end for
18: end for
19: 对标记区域进行形态学膨胀（dilation, 半径0.3m），确保机器人不会"擦边"经过坑洞边缘
20: return C_negative_obs
```

**物理原理：** 激光束在到达坑洞"近壁"时返回第一个回波（距离 $d_\text{near}$），然后激光束穿越坑洞空间到达"远壁"时返回第二个回波（距离 $d_\text{far}$）。在单回波LiDAR的情况下，看到的是 $d_\text{near}$（近壁），远壁可能因多回波抑制而丢失。因此，主要依赖手段是：一旦判断某处有距离不连续性，就在体素栅格中查询该处的**垂直结构**：如果体素列在 $z_\text{ground}$ 以下有大量UNKNOWN空间（深度 > 0.3m），则判定为负障碍物。

**（e）粗糙度评估（Roughness Estimation）：**

对于非结构化地形（碎石地、瓦砾），即使步高和坡度在阈值内，地面的高频不平整（粗糙度）也可能导致机器人剧烈振动，影响传感器数据质量和定位精度。粗糙度评估计算局部窗口内地表体素的**高程标准差**：
$$\sigma_z(x, y) = \sqrt{\frac{1}{|\mathcal{N}(x, y)|} \sum_{(x', y') \in \mathcal{N}(x, y)} (z(x', y') - \bar{z})^2}$$

其中 $\mathcal{N}(x, y)$ 为 $(x, y)$ 周围 0.3m 半径内的地面体素。

若 $\sigma_z > \sigma_\text{max}$（典型值 0.05m），该区域代价按比例增加：$\mathcal{C}_\text{terrain}(x, y) = 254 \cdot \min(1, \sigma_z / \sigma_\text{max})$。

---

### 5. 动态可见性图 —— FAR (Frontier-based Adaptive Re-planner)

FAR是嵌入TARE全局规划层中的一个**动态可见性图（Dynamic Visibility Graph）**模块，在复杂拓扑的狭窄迷宫式环境中弥补纯A*规划器的不足。

**（a）动机与问题：**

在地下环境（特别是矿井和洞穴）中，机器人的行进路径可能被暂时不可见的障碍物或未知区域阻断。传统的全局规划器（A* on occupancy grid）需要显式的占据/自由体素信息才能搜索路径，但在探索过程中：
- 某些区域标记为UNKNOWN（被其他障碍物遮挡）
- 如果A*在UNKNOWN区域周围找不到绕行路径，会报告"无路径可达"（no path found）
- 但实际可能通过尚未被观测的通道连接

FAR通过在**前沿点之间构建可见性图**，为TARE的全局引导提供了超越占据栅格的拓扑连通性推理。

**（b）FAR的图构建：**

1. **前沿点检测：** 在体素栅格中提取所有 FREE-UNKNOWN 边界体素，聚类为前沿区域 $\mathcal{F} = \{F_1, F_2, \dots, F_K\}$
2. **节点定义：** 每个前沿区域的质心作为图节点 $n_i = \text{Centroid}(F_i)$
3. **边定义：** 对于任意节点对 $(n_i, n_j)$，若二者之间的视线（Line-of-Sight, LoS）**不被占据体素阻断**，则添加无向边 $e_{ij}$，其代价为欧几里得距离 $\|n_i - n_j\|$ + 沿途UNKNOWN体素的惩罚项（鼓励机器人沿已知区域穿越）：
   $$w(e_{ij}) = \|n_i - n_j\| + \lambda_\text{unknown} \cdot \frac{\text{UNKNOWN体素数在LoS上}}{\text{LoS总体素数}} \cdot \|n_i - n_j\|$$
4. **动态更新：** 随着探索推进，新前沿区域被发现，旧前沿区域被标记为已知并移除，FAR图增量更新（增量边添加/移除，而非全图重建）

**（c）FAR在系统中的角色：**

FAR不替代TARE的A*全局规划器，而是**平行运行**的一个补充模块。当A*报告"无路径可达当前目标"时，TARE查询FAR图：在图中搜索从当前节点到目标前沿节点的最短路径。FAR的边不要求完全占据/自由验证（仅要求视线不被占据体素阻断），使其在部分未知环境中具有更强的连通性推理能力。

**（d）FAR的计算复杂度：**

| 操作                | 复杂度                                   | 典型耗时            | 触发条件       |
| ----------------- | ------------------------------------- | --------------- | ---------- |
| 前沿点聚类             | $\mathcal{O}(N_\text{voxel})$         | 15 ms           | 新LiDAR扫描到达 |
| 节点对LoS检查          | $\mathcal{O}(K^2 \cdot L_\text{max})$ | 10 ms ($K=20$)  | 前沿点集合变化    |
| 最短路径搜索 (Dijkstra) | $\mathcal{O}(K^2)$                    | < 1 ms ($K=20$) | 按需（A*失败时）  |

---

### 6. DARPA SubT实战验证

CMU团队将上述完整系统部署在DARPA地下挑战赛（Subterranean Challenge）的三个赛道中，取得了卓越成绩。

**（a）赛道概述：**

| 赛道                       | 环境特征                    | 难度特征                             |
| ------------------------ | ----------------------- | -------------------------------- |
| **Tunnel Circuit（隧道赛道）** | 废弃矿井、狭窄隧道（宽度1-3m）、长距离单向 | 极端黑暗，粉尘，无GPS，通信延迟                |
| **Urban Circuit（城市赛道）**  | 废弃核电站、多楼层建筑、楼梯、管道       | 三维复杂拓扑，多材质表面（金属/混凝土），动态障碍物（掉落碎片） |
| **Cave Circuit（洞穴赛道）**   | 天然石灰岩洞穴、不规则地形、水坑        | 极度不规则地形，负障碍物密集，高湿度导致LiDAR衰减      |

**（b）Urban Circuit 详细成绩：**

CMU在Urban Circuit中取得了标杆性成绩：
- **自主探索距离：886米**
- **自主运行时间：1458秒（24分18秒）**
- **探索扇区数：26/28（"Most Sectors Explored Award"）**
- **人工干预次数：0**
- **碰撞事故：0**
- **机器人损毁：0**

**（c）TARE探索规划器在比赛中的具体贡献：**

| 指标     | TARE贡献                                       | 量化证据                             |
| ------ | -------------------------------------------- | -------------------------------- |
| 探索覆盖率  | TARE的全局-局部分层规划使机器人在不重复穿越已知区域的前提下系统性地覆盖未知区域   | Urban Circuit 26/28扇区探索，是场地中最高纪录 |
| 计算效率   | TARE的平均规划时间（210ms）在NUC i7上远低于NBVP的4.6s       | 在同样时间内，TARE可完成21次规划循环，NBVP仅1次    |
| 负障碍物处理 | FAR + 地形分析的多层安全机制在Cave Circuit中避免了3次检测到的竖井坠落 | 赛后日志分析确认FAR在3处提前改道规避了直径>1m的竖井    |
| 回程效率   | TARE的Relocation Mode使机器人在比赛倒计时阶段能快速返回        | 最后一次回程中，回程路径比原探索路径短38%           |
| 拓扑适应性  | TARE拓扑图在复杂地下多岔道环境中维护了正确的连通性表示                | 比赛全程拓扑图节点数273，边数412，无错误连通        |

**（d）系统瓶颈分析与改进方向：**

从比赛日志的事后分析中，CMU团队识别出以下瓶颈：

| 瓶颈                | 发生次数                       | 影响                 | 已/将改进                 |
| ----------------- | -------------------------- | ------------------ | --------------------- |
| 地形分析假阴性（未检测到危险）   | 1次（Cave Circuit，机器人在碎石中打滑） | 轻微（机器人自动恢复稳定，未翻倒）  | 集成IMU实时倾斜检测作为独立安全层    |
| 全局规划器路径缓存失效       | 多次（当刚爆炸的碎石改变了环境拓扑）         | 中等（触发全图重规划，延迟3-5s） | 启用增量式拓扑图更新 + 局部路径修正   |
| 通信中断时多机器人协调失败     | 数次（Tunnel Circuit，信号衰减）    | 中等（两机器人偶然探索重叠区域）   | 增加分布式前沿分配协议           |
| FALCO在极端非凸地形中的子优性 | 2-3次                       | 轻微（路径绕远，但未碰撞）      | 增加运动基元数量 $M$（500→900） |

---

## 三、论文核心贡献

### 1. 方法学核心创新

**（a）首个完整的开源三层自主探索系统。** 本文将TARE全局探索规划器、FALCO局部规划器、地形可通行性分析三个独立设计的算法首次集成为统一的、互操作的、端到端的开源软件栈（基于ROS），包含所有模块的公开源代码、仿真配置和部署文档。该系统填补了"从独立算法到可部署系统"之间的工程鸿沟，为社区提供了一个可参考、可复现、可扩展的完整基线。

**（b）FALCO：针对非结构化地形的采样型局部规划器。** FALCO以蒙特卡洛运动基元采样替代传统梯度优化，从根本上规避了非凸环境中的局部最小值问题。其计算时间严格可预测（< 5ms），适合高频控制回路。与TEB/MPC等梯度方法的对比在非结构化环境中具有明显优势。

**（c）负障碍物检测与多层代价地图的安全体系。** 地形分析模块系统性地处理了地下环境特有的安全威胁（负障碍物、陡坡、台阶），并通过多层代价地图的最大值融合策略保证了安全优先的设计哲学。负障碍物检测算法是在DARPA SubT实战压力下催生的、经物理验证的关键安全功能。

**（d）FAR动态可见性图在未知环境中的连通性推理。** FAR通过在拓扑前沿点之间构建可见性图，为TARE的全局引导提供了能处理"部分未知环境"的补充连通性推理，弥补了纯占据栅格A*在未探索区域的局限性。

### 2. 理论体系贡献

| 贡献项                      | 核心内容                                                                  | 理论/工程意义                                      |
| ------------------------ | --------------------------------------------------------------------- | -------------------------------------------- |
| **三层频率解耦架构的形式化**         | 全局层 0.2-0.5Hz, 局部层 5-20Hz, 地形层 10-30Hz                                | 建立了探索系统频率分层设计的标准范式，未来系统可按相同频率分配设计新模块         |
| **采样型vs.梯度型局部规划器的理论对比**  | 采样: $\mathcal{O}(M \cdot N)$ 确定性; 梯度: $\mathcal{O}(T \cdot n^3)$ 收敛依赖 | 从渐进复杂度角度论证了采样型规划器在实时约束下对梯度型的优势               |
| **多层代价图的max-fusion安全理论** | $\mathcal{C} = \max(\mathcal{C}_1, \dots, \mathcal{C}_K)$ 保证安全优先      | 证明了max-fusion在安全关键系统中的单调性：添加新层不会降低任何区域的安全性评级 |
| **负障碍物的生成模型**            | $\Delta d > \tau \land \text{vertical\_gap}(z) > h_\text{min}$        | 将负障碍物检测形式化为距离不连续性与垂直空隙的联合概率推理                |
| **前沿点可见性图的近似完备性**        | FAR保证在自由空间连通的假设下，任何前沿点对间存在连通路径                                        | 为未知环境中的全局引导提供了拓扑层面的理论保证                      |

### 3. 工程与产业价值贡献

**（a）DARPA SubT "Most Sectors Explored Award" 的产业化意义：**
CMU团队在DARPA SubT Urban Circuit中以26/28的探索扇区数获得最高探索覆盖奖，该成果直接证明了开源三层导航架构在极端真实环境中的有效性和鲁棒性。DARPA SubT是全球最负盛名的机器人地下挑战赛，其结果对国防、矿难救援、核设施巡检等产业领域具有直接参考价值。

**（b）开源软件栈（github.com/cmu-exploration）：**
CMU将完整系统作为开源项目发布，包含：

| 模块                        | 仓库                | 核心功能                           | 语言          | 依赖                |
| ------------------------- | ----------------- | ------------------------------ | ----------- | ----------------- |
| `tare_planner`            | 独立包               | TARE全局探索规划器（局部+全局+拓扑）          | C++         | ROS, PCL, Eigen   |
| `falco_planner`           | 独立包               | FALCO局部运动基元规划器                 | C++         | ROS, costmap_2d   |
| `terrain_analysis`        | cmu-exploration元包 | 地形可通行性分析（坡度/步高/负障碍/粗糙度）        | C++         | ROS, PCL, OctoMap |
| `far_planner`             | 独立包               | FAR动态可见性图规划器                   | C++         | ROS               |
| `exploration_manager`     | cmu-exploration元包 | 顶层状态机，协调三层规划与模式切换              | Python/C++  | ROS               |
| `cmu_exploration_bringup` | cmu-exploration元包 | 仿真/真实的launch文件、参数文件、Dockerfile | YAML/Docker | Gazebo/Docker     |

**（c）工业级部署验证（详见表I-IV）：**

**表I —— Urban Circuit 完整性能指标**

| 指标               | 值                 | 备注                                      |
| ---------------- | ----------------- | --------------------------------------- |
| 自主探索距离           | 886 m             | 在未知废弃核电站中的总行进距离                         |
| 自主运行时间           | 1458 s (24.3 min) | 零人工干预                                   |
| 探索扇区数            | 26 / 28           | **全场最高**（"Most Sectors Explored Award"） |
| 平均速度             | 0.61 m/s          | 含停车探索评估时间                               |
| 纯行进速度            | 1.26 m/s          | 不含停车评估                                  |
| 碰撞次数             | 0                 | —                                       |
| 机器人损毁            | 0                 | —                                       |
| TARE 全局规划周期      | 210 ms (平均)       | 标准差 ±45ms                               |
| FALCO 局部规划周期     | 4.8 ms (平均)       | 标准差 ±1.2ms                              |
| 地形分析周期           | 18 ms (平均)        | 标准差 ±5ms                                |
| 总CPU使用率 (NUC i7) | 62% (平均)          | 4核, 峰值78%                               |

**表II —— 地形分析对安全性的影响（Cave Circuit 消融实验）**

| 配置              | 碰撞次数        | 险情次数 (需人工紧急制动) | 探索距离       | 成功率      |
| --------------- | ----------- | -------------- | ---------- | -------- |
| **完整系统（含地形分析）** | **0**       | **0**          | 620m       | **100%** |
| 移除负障碍物检测        | 1（坠入0.4m浅坑） | 3              | 598m       | 90%      |
| 移除步高检测          | 2（卡在台阶）     | 5              | 565m       | 82%      |
| 移除所有地形分析        | 4           | 11             | 487m（测试终止） | 55%      |

说明：负障碍物检测在Cave Circuit中预防了3次可能致命的竖井坠落（根据赛后日志，3处竖井直径均>1m，深度>3m），地形分析整体将安全性提升了数个等级。

**表III —— FALCO vs. TEB 在非结构地形中的性能对比（8次测试均值）**

| 指标             | FALCO (M=500基元) | TEB (g2o优化)                      | FALCO优势               |
| -------------- | --------------- | -------------------------------- | --------------------- |
| 单次规划耗时         | 4.8 ms          | 18.3 ms (中位数), 42.1 ms (95th百分位) | 3.8× (中位数), 8.8× (尾部) |
| 碰撞率 (per 100m) | 0.02            | 0.11                             | 5.5×                  |
| 路径平滑度 (平均曲率)   | 0.38 m⁻¹        | 0.22 m⁻¹                         | TEB更平滑（1.7×）          |
| 在非凸障碍中的成功率     | 98%             | 76%                              | +22%                  |
| 对非结构地形的成功率     | 100%            | 88%                              | +12%                  |
| CPU使用          | 8%              | 22%                              | 2.8×                  |

说明：TEB在路径平滑度上优于FALCO（梯度优化天然产生更平滑路径），但在非凸/非结构环境中，TEB的梯度优化极易陷入不可行解（成功率的22%和12%差距），而FALCO的采样策略天然免疫该问题。FALCO的计算时间方差极小（std=1.2ms），适合安全关键系统的实时约束。

**表IV —— 系统开源以来的社区采用指标（截至论文发表）**

| 指标                             | 值                            |
| ------------------------------ | ---------------------------- |
| GitHub Stars (CMU Exploration) | 1,200+                       |
| 独立部署团队数（已知）                    | 45+ (含DARPA SubT其他团队)        |
| ROS Discourse 讨论帖数             | 80+                          |
| 引用次数（截至ICRA 2022）              | 引用TARE的出版物 > 50，引用完整系统的 > 30 |
| 外部贡献者                          | 15+ (中国、韩国、德国、瑞士等)           |

**（d）最佳实践指南（Best Practices Guide）摘要：**

论文的附录提供了一份针对自主探索系统开发者的最佳实践指南，核心建议如下：

1. **分层是系统鲁棒性的第一原则：** 按频率解耦全局探索（0.2-0.5Hz）、局部规划（5-20Hz）、地形安全（10-30Hz），不跨层混用计算资源
2. **采样-评估优于梯度优化（在非结构化环境中）：** 运动基元采样提供了可预测的计算时间和非凸鲁棒性，代价是路径平滑度略降。当安全性优先于路径最优性时，选择采样范式
3. **代价地图的最大值融合是安全关键系统的最小化原则：** 不要使用加权求和——当任何一个安全层报告危险时，系统必须无条件信任该报告
4. **负障碍物检测是地下（和任何非平坦环境）部署的必备模块：** 立体视觉和LiDAR的俯角限制使得机器人脚下的坑洞天然不可见，必须依赖距离不连续性 + 体素空隙分析的主动检测
5. **仿真中充分测试后，必须预留至少30%的真实环境调试时间：** 传感器噪声、地面摩擦、光照变化在仿真中无法完美模拟，真实调试中发现的问题通常需要对软件栈的非平凡修改
6. **通信中断的假设必须内置在系统架构中：** 地下环境无GPS/WiFi，遥操作带宽极低（< 1KB/s），架构必须假设机器人99.9%的时间完全自主运行
7. **模块化软件 + 独立包发布：** 每个算法模块以独立的ROS包发布（而非单一的单体仓库），允许社区选择性采用（如只使用FALCO而不使用TARE），最大化开源影响力

**（e）产业合作伙伴：**
CMU探索团队在DARPA SubT中的合作伙伴包括**Boeing（波音）、NVIDIA、Ouster（激光雷达供应商）、FLIR（热成像传感器）**，这些工业伙伴为系统提供了硬件定制、计算加速（NVIDIA Jetson平台）和传感器优化（Ouster提供专属驱动），表明了该系统从学术原型向工业产品转化的可行路径。
