1. minPathRange 调得太大会怎么样？

minPathRange 是最小路径范围（单位：米），它决定了规划器至少会向前看多远来搜索可行驶路径。即使车辆速度很低甚至静止，路径也不会短于这个值。

调得太大的后果（例如设为 10 米）：

    穿过障碍物：如果车辆起点附近就有障碍物（比如 3 米外有一堵墙），规划器会强制寻找一条长度 ≥10 米的路径。由于没有足够空间，它可能会选择一条穿过障碍物的路径（因为评价时只看整条路径上的命中点数，而 10 米长路径中可能大部分是空的，仅初始段有障碍物，总命中点数可能低于阈值，从而被错误判定为“可行”）。

    路径诡异：为了达到那么长的范围，规划器会倾向于选择那些拐弯后绕远的路径（即使实际不需要走那么远），导致车辆绕路。

    计算负担增加：处理更长的路径点集，且障碍物检测需要覆盖更大距离，耗时增加。

正常情况下，minPathRange 应略大于车辆从当前速度到刹停所需的距离（比如 1.5~2.0 米），保证能安全避障即可。
2. 为什么 pathCropByGoal = false 时路径可能会“远于目标点”？

参数 pathCropByGoal 控制是否根据目标点的距离裁剪路径。

    true（默认）：规划器只保留路径中距离起点 ≤ 目标点距离的那一段。如果目标点就在 2 米处，路径上 2 米以后的点会被丢弃。

    false：保留整条路径（可能长达 pathRange，例如 4.5 米）。这时即使目标点只有 2 米远，发布的路径依然会延伸到 4.5 米外。

“远于目标点”的含义：车辆收到一条路径，其末端超出了目标位置。但控制器（pathFollower）不会自动在目标点停下，它只会一直跟踪路径末端。结果就是车辆会冲过目标点，沿着路径多余的尾巴继续走，造成“过冲”。
为什么有时要设为 false：在障碍物密集区，裁剪掉远处路径可能导致所有路径都被提前截断而“无路可走”。保留长路径可让规划器避开局部障碍物后再回到目标方向。
3. goalX / goalY 到底是什么意思？

在代码中，这两个变量存储的是目标点在全局坐标系下的坐标（单位：米）。
它们有两个来源：

    固定值：通过 launch 文件传入的 <arg name="goalX" default="0"/> 和 <arg name="goalY" default="0"/>。如果您没有动态发布目标点，车辆就会一直试图开向 (0, 0)（即原点）。

    动态更新：通过订阅 /way_point 话题（goalHandler 回调），话题消息类型为 geometry_msgs/PointStamped。当您用 Rviz 或其他工具发布一个点时，goalX 和 goalY 会被更新为该点的坐标。

在导航中的作用：当 autonomyMode = true 时，规划器会计算车辆前方到目标点的方向角（相对于车辆朝向），并用这个方向作为期望行驶方向（joyDir）。然后从预定义路径库中选择最接近该方向的可行路径。所以 goalX/goalY 是自主导航的目标点，不是路径的终点，而是方向指引。
4. “far planner 里面目标点不是我手动创建的吗？”

您说的“far planner”可能是指这个 local_planner 节点。目标点确实需要您手动创建，有两种方式：

    静态：修改 launch 文件中的 goalX 和 goalY 默认值。

    动态：使用 Rviz 的 “2D Nav Goal” 工具发布 /way_point 消息，或者在您的代码中发布该话题。

常见误解：有些用户以为目标点是路径终点（即车辆最终停下的精确位置）。实际不是——目标点只提供期望朝向，车辆会持续沿路径运动，即使已经超过目标点，除非控制器中有额外的停止逻辑（pathFollower 中的 stopDisThre 参数才会在距离目标点足够近时停车）。所以如果您希望车辆精确停在 (goalX, goalY)，还需要配合 pathFollower 的停车距离参数。
总结建议

    保持 minPathRange 在 1.5～2.5 米，不要超过邻近范围的一半。

    若发现车辆在障碍物附近无路可走，可尝试 pathCropByGoal = false，同时调大 pointPerPathThre 避免穿过障碍物。

    确认目标点发布：在 Rviz 中点击 “2D Nav Goal” 后，观察终端是否有 /way_point 话题的打印，或使用 rostopic echo /way_point 验证。
